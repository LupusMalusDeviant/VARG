#![allow(
    unused_variables,
    unused_mut,
    dead_code,
    unused_imports,
    unreachable_code,
    unused_assignments
)]

// --- AUTOGENERATED BY Varg Compiler ---
use varg_os_types::*;
use varg_runtime::*;

fn process_item(data: String) -> String {
    // .varg:1
    return format!("processed:{}", data);
}

fn validate(input: String) -> bool {
    // .varg:2
    return input.len() as i64 > 0;
}

struct Pipeline {
    pub last_log: String,
    pub log_count: i64,
    pub items_processed: i64,
    pub status: String,
}

impl Pipeline {
    pub fn new() -> Self {
        let mut __self = Pipeline {
            last_log: String::new(),
            log_count: 0,
            items_processed: 0,
            status: String::new(),
        };
        __self.Init();
        __self
    }
    pub fn Init(&mut self) {
        // .varg:3
        self.last_log = "".to_string();
        // .varg:4
        self.log_count = 0;
        // .varg:5
        self.items_processed = 0;
        // .varg:6
        self.status = "idle".to_string();
    }
    pub fn LogMsg(&mut self, msg: String) {
        // .varg:7
        self.log_count += 1;
        // .varg:8
        self.last_log = format!("[{}] {}", self.log_count, msg);
        // .varg:9
        println!("{:?}", self.last_log);
    }
    pub fn Process(&mut self, data: String) -> String {
        // .varg:10
        self.status = "processing".to_string();
        // .varg:11
        let mut result = process_item(data.clone());
        // .varg:12
        self.items_processed += 1;
        // .varg:13
        self.status = "done".to_string();
        // .varg:14
        return result;
    }
    pub fn Run(&mut self) {
        // .varg:15
        self.LogMsg("Starting pipeline".to_string());
        // .varg:16
        let mut datasets = vec![
            "alpha".to_string(),
            "beta".to_string(),
            "gamma".to_string(),
            "delta".to_string(),
        ];
        // .varg:17
        for mut item in datasets {
            // .varg:18
            let mut is_valid = validate(item.clone());
            // .varg:19
            if is_valid {
                // .varg:20
                let mut result = self.Process(item.clone());
                // .varg:21
                self.LogMsg(format!("Processed: {}", result));
            }
        }
        // .varg:22
        self.LogMsg(format!("Total processed: {}", self.items_processed));
        // .varg:23
        let mut final_result = self.Process("critical-data".to_string());
        // .varg:24
        self.LogMsg(format!("Final response: {}", final_result));
        // .varg:25
        let mut summary = format!(
            "Pipeline: {} items, {} logs, status={}",
            self.items_processed, self.log_count, self.status
        );
        // .varg:26
        println!("{:?}", summary);
        // .varg:27
        if self.items_processed > 3 {
            // .varg:28
            println!("{}", "High throughput".to_string());
        } else if self.items_processed > 1 {
            // .varg:29
            println!("{}", "Normal throughput".to_string());
        } else {
            // .varg:30
            println!("{}", "Low throughput".to_string());
        }
    }
}
