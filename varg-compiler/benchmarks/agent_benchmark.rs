#![allow(
    unused_variables,
    unused_mut,
    dead_code,
    unused_imports,
    unreachable_code,
    unused_assignments
)]

// --- AUTOGENERATED BY Varg Compiler ---
use varg_os_types::*;
use varg_runtime::*;

trait DataProcessor {
    fn Process(&mut self, input: String) -> String;
    fn GetCount(&mut self) -> i64;
}

fn clean_text(text: String) -> String {
    // .varg:1
    return text.trim().to_string();
}

struct ApiProcessor {
    pub last_result: String,
    pub request_count: i64,
}

impl ApiProcessor {
    pub fn new() -> Self {
        let mut __self = ApiProcessor {
            last_result: String::new(),
            request_count: 0,
        };
        __self.Init();
        __self
    }
    pub fn Init(&mut self) {
        // .varg:2
        self.last_result = "".to_string();
        // .varg:3
        self.request_count = 0;
    }
    pub fn Process(&mut self, input: String) -> String {
        // .varg:4
        self.request_count += 1;
        // .varg:5
        let mut cleaned = clean_text(input.clone());
        // .varg:6
        self.last_result = format!("[{}] {}", self.request_count, cleaned);
        // .varg:7
        return self.last_result.clone();
    }
    pub fn GetCount(&mut self) -> i64 {
        // .varg:8
        return self.request_count.clone();
    }
    pub fn Run(&mut self) {
        // .varg:9
        let mut i = 0;
        // .varg:10
        while i < 5000 {
            // .varg:11
            let mut input = format!("  Request {}  ", i);
            // .varg:12
            self.Process(input.clone());
            // .varg:13
            i += 1;
        }
        // .varg:14
        println!("{}", format!("Processed {} requests", self.request_count));
        // .varg:15
        println!("{}", format!("Last: {}", self.last_result));
    }
}

impl DataProcessor for ApiProcessor {
    fn Process(&mut self, input: String) -> String {
        // .varg:16
        self.request_count += 1;
        // .varg:17
        let mut cleaned = clean_text(input.clone());
        // .varg:18
        self.last_result = format!("[{}] {}", self.request_count, cleaned);
        // .varg:19
        return self.last_result.clone();
    }
    fn GetCount(&mut self) -> i64 {
        // .varg:20
        return self.request_count.clone();
    }
}
