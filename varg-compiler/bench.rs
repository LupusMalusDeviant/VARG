// --- AUTOGENERATED BY Varg Compiler ---
use varg_os_types::*;
use varg_runtime::*;

struct Benchmark {}

impl Benchmark {
    pub fn RunOnce(&mut self) {
        let mut n = 35;
        let mut fa = 0;
        let mut fb = 1;
        let mut fi = 0;
        while fi < n {
            let mut temp = fb;
            fb = fa + fb;
            fa = temp;
            fi = fi + 1;
        }
        let mut result = "".to_string();
        let mut si = 0;
        while si < 10000 {
            result.push_str("x");
            si = si + 1;
        }
        let mut numbers = vec![0];
        let mut ai = 1;
        while ai < 10000 {
            numbers.push(ai);
            ai = ai + 1;
        }
        let mut sum = 0;
        let mut mi = 0;
        while mi < 10000 {
            sum = sum + numbers[mi as usize];
            mi = mi + 1;
        }
    }
    pub fn Run(&mut self) {
        println!("{}", "=== Varg Benchmark (100 iterations) ===".to_string());
        let mut iter = 0;
        while iter < 100 {
            self.RunOnce();
            iter = iter + 1;
        }
        println!("{}", "Fibonacci(35) = 9227465".to_string());
        println!("{}", "String length: 10000".to_string());
        println!("{}", "Sum 0..9999 = 49995000".to_string());
        println!("{}", "=== Done ===".to_string());
    }
}

