#![allow(
    unused_variables,
    unused_mut,
    dead_code,
    unused_imports,
    unreachable_code,
    unused_assignments
)]

// --- AUTOGENERATED BY Varg Compiler ---
use varg_os_types::*;
use varg_runtime::*;

fn fib(n: i64) -> i64 {
    // .varg:1
    if n <= 1 {
        // .varg:2
        return n;
    }
    // .varg:3
    return fib(n - 1) + fib(n - 2);
}

struct Benchmark {}

impl Benchmark {
    pub fn Run(&mut self) {
        // .varg:4
        let mut start = (std::time::SystemTime::now()
            .duration_since(std::time::UNIX_EPOCH)
            .unwrap()
            .as_millis() as i64);
        // .varg:5
        let mut result = fib(35);
        // .varg:6
        let mut elapsed = (std::time::SystemTime::now()
            .duration_since(std::time::UNIX_EPOCH)
            .unwrap()
            .as_millis() as i64)
            - start;
        // .varg:7
        println!("{}", format!("fib(35) = {}", result));
        // .varg:8
        println!("{}", format!("Time: {}ms", elapsed));
    }
}

fn main() {
    let _varg_args: Vec<String> = std::env::args().collect();
    let mut instance = Benchmark {};
    println!("[VargOS] Bootstrapping Runtime...");
    instance.Run();
}
