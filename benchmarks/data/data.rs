#![allow(
    unused_variables,
    unused_mut,
    dead_code,
    unused_imports,
    unreachable_code,
    unused_assignments
)]

// --- AUTOGENERATED BY Varg Compiler ---
use varg_os_types::*;
use varg_runtime::*;

fn main() {
    // .varg:1
    let mut start = (std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .unwrap()
        .as_millis() as i64);
    // .varg:2
    let mut total = 0;
    // .varg:3
    let mut count = 0;
    // .varg:4
    let mut i = 0;
    // .varg:5
    while i < 100000 {
        // .varg:6
        if i % 2 == 0 {
            // .varg:7
            total += i * 2;
            // .varg:8
            count += 1;
        }
        // .varg:9
        i += 1;
    }
    // .varg:10
    let mut words = vec![
        "rust".to_string(),
        "varg".to_string(),
        "ai".to_string(),
        "agent".to_string(),
        "compile".to_string(),
        "type".to_string(),
        "safe".to_string(),
        "fast".to_string(),
        "native".to_string(),
        "async".to_string(),
    ];
    // .varg:11
    let mut freq = std::collections::HashMap::from([
        ("rust".to_string(), 0),
        ("varg".to_string(), 0),
        ("ai".to_string(), 0),
        ("agent".to_string(), 0),
        ("compile".to_string(), 0),
        ("type".to_string(), 0),
        ("safe".to_string(), 0),
        ("fast".to_string(), 0),
        ("native".to_string(), 0),
        ("async".to_string(), 0),
    ]);
    // .varg:12
    let mut j = 0;
    // .varg:13
    while j < 10000 {
        // .varg:14
        let mut word = words[j % 10 as usize];
        // .varg:15
        freq.insert(word, freq.get(&word).cloned().unwrap_or(0) + 1);
        // .varg:16
        j += 1;
    }
    // .varg:17
    let mut elapsed = (std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .unwrap()
        .as_millis() as i64)
        - start;
    // .varg:18
    println!("{}", format!("Sum: {}", total));
    // .varg:19
    println!(
        "{}",
        format!(
            "Freq entries: {}",
            freq.keys().cloned().collect::<Vec<_>>().len() as i64
        )
    );
    // .varg:20
    println!("{}", format!("Time: {}ms", elapsed));
}
